name: Test

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
      types: [opened, labeled, reopened, synchronize]

  push:
    branches:
    - "trunk"

  workflow_dispatch:

jobs:
  start_action_runners:
    name: Start
    uses: glotzerlab/jetstream2-admin/.github/workflows/start.yaml@f6746b0a76c06e45cc1b0c5e32576dfa2c7d2d8c # v1.5.1
    secrets: inherit

  typical:
    name: "${{ join(matrix.config, '') }}"
    needs: start_action_runners
    uses: ./.github/workflows/build_and_test.yaml
    with:
      compiler_family: ${{ matrix.config[0] }}
      compiler_version: ${{ matrix.config[1] }}
      python: ${{ matrix.config[3] }}
      config: ${{ join(matrix.config, '') }}

      # Use self-hosted runners or fall back to GitHub hosted runners when self-hosted are offline.
      build_runner: ${{ needs.start_action_runners.outputs.active != '0' && '["self-hosted","jetstream2","CPU"]' || toJson('ubuntu-24.04') }}

      # Default to false when unset
      validate: ${{ matrix.validate && 'true' || 'false'}}

      release: false

    strategy:
      fail-fast: false
      matrix:
        include:
        - config: [clang, 18, -py, 314, -mpi]
          validate: true

        - config: [gcc, 14, -py, 314]
          validate: true

        - config: [gcc, 14, -py, 314, -nomd]
        - config: [gcc, 14, -py, 314, -nohpmc]
        - config: [gcc, 14, -py, 314, -nomd, -nohpmc]
        - config: [gcc, 10, -py, 311, -mpi]

        - config: [cuda, 124, -py, 314, -mpi]
          validate: true

        - config: [cuda, 124, -py, 314]
          validate: true

        - config: [cuda, 124, -py, 314, -mpi, -debug]

        - config: [rocm, 72, -py, 313]
        - config: [rocm, 71, -py, 313]
        - config: [rocm, 64, -py, 313]


  release:
    if: ${{ contains(github.event.pull_request.labels.*.name, 'release') }}
    name: "${{ join(matrix.config, '') }}"
    needs: start_action_runners
    uses: ./.github/workflows/build_and_test.yaml
    with:
      compiler_family: ${{ matrix.config[0] }}
      compiler_version: ${{ matrix.config[1] }}
      python: ${{ matrix.config[3] }}
      config: ${{ join(matrix.config, '') }}

      # Use self-hosted runners or fall back to GitHub hosted runners when self-hosted are offline.
      build_runner: ${{ needs.start_action_runners.outputs.active != '0' && '["self-hosted","jetstream2","CPU"]' || toJson('ubuntu-24.04') }}

      # Default to false when unset
      validate: ${{ matrix.validate && 'true' || 'false'}}

      release: true

    strategy:
      fail-fast: false
      matrix:
        include:
        - config: [clang, 17, -py, 313, -mpi]
        - config: [clang, 17, -py, 312, -mpi]
        - config: [clang, 16, -py, 312, -mpi]
        - config: [clang, 15, -py, 311, -mpi]
        - config: [clang, 14, -py, 311, -mpi]
        - config: [clang, 13, -py, 311, -mpi]
        - config: [gcc, 13, -py, 313, -mpi]
        - config: [gcc, 12, -py, 312, -mpi]
        - config: [gcc, 11, -py, 311, -mpi]

  tests_complete:
    name: Unit test
    if: always()
    needs: [typical]
    runs-on: ubuntu-24.04

    steps:
    - run: jq --exit-status 'all(.result == "success")' <<< '${{ toJson(needs) }}'
    - name: Done
      run: exit 0
