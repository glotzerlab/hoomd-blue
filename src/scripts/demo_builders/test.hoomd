from hoomd_script import *

init.read_xml('test.xml')

lj = pair.lj(r_cut=1.0)
lj.pair_coeff.set('A', 'A', epsilon=1.0, sigma=0.890899, alpha=0.0)
lj.pair_coeff.set('A', 'N', epsilon=1.0, sigma=0.890899, alpha=0.0)
lj.pair_coeff.set('N', 'N', epsilon=1.0, sigma=0.890899, alpha=0.0)

#harmonic = bond.harmonic();
#harmonic.set_coeff('block', r0=1.0*1.0, k=100000)
#harmonic.set_coeff('cross', r0=1.7320508075689*1.0, k=100000)

force.constant(0, -9.8, 0)

wall_lj = wall.lj(r_cut=3.0)
wall_lj.set_coeff('A', epsilon=0.0, sigma=1.0, alpha=1.0)
wall_lj.set_coeff('N', epsilon=1.0, sigma=1.0, alpha=1.0)

dump.mol2('test.mol2')
dump.dcd('dump.dcd', period=10)

integrate.nve(dt=0.001);
run(10000)
